# 密钥 / 本地配置 —— 任意目录下都不提交
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
lsh_web/

# PyInstaller 构建产物（用 build_exe.py 重新生成；需提交时用 git add -f）
build/
dist/
*.spec
发布/

# 本地工具与工作区（imgupload/config.json 内含博客园明文令牌，绝不可提交）
imgupload/
.workbuddy/
